# Constraints do design `hello`.
# Ele e puramente combinacional (nao tem clock), entao nao ha
# create_clock / set_input_delay / set_output_delay aqui.
# O arquivo existe mesmo assim porque o synth.tcl faz read_sdc dele
# (mesma convencao do repositorio porta-and do instrutor).
